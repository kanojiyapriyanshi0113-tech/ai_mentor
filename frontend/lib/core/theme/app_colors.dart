import "package:flutter/material.dart";

/// Centralized color palette for the AI Mentor design system.
class AppColors {
  AppColors._();

  // Primary brand purple
  static const Color primary = Color(0xFF6C4CE0);
  static const Color primaryDark = Color(0xFF5136B8);
  static const Color primaryLight = Color(0xFFF2EEFF);

  // Secondary accent
  static const Color secondary = Color(0xFF9C7BFF);

  // Semantic
  static const Color success = Color(0xFF2E9E5B);
  static const Color warning = Color(0xFFE0A800);
  static const Color error = Color(0xFFDC3545);
  static const Color info = Color(0xFF3D8BFF);

  // Neutrals
  static const Color background = Color(0xFFFAF9FC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF3F1F8);
  static const Color textPrimary = Color(0xFF1D1B24);
  static const Color textSecondary = Color(0xFF6E6B7A);
  static const Color border = Color(0xFFE6E3EF);
}
